// problem 11 :Greetings: Start with the array you used in Exercise 11,
//  but instead of just printing each person’s name, print a message to them.
//   The text of each message should be the same, but each message should be
// personalized with the person’s name.

const namesArray = ['ali', 'daniyal', 'khurram']
for (let index = 0; index < namesArray.length; index++) {
  const element = namesArray[index]
  console.log(element,"hi programmer")
}
